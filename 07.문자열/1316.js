// 그룹 단어 체커

let fs = require('fs');
let input = fs.readFileSync('/dev/stdin').toString().split('\n');

const n = input[0];
const testList = input.slice(1, input.length);
let cnt = 0;

for (let i = 0; i < n; i++) {
  for (let j = 0; j < testList.length; j++) {}
}
